package es.upo.tfg.rol.model.dao;

import org.springframework.stereotype.Repository;

import es.upo.tfg.rol.model.pojos.User;

//@Repository("userDAO")
//public class UserDAOImpl extends AbstractHibernateDAO<User> implements UserDAO { // Implements userDAO
//
//	@Override
//	public User findByNic(String nickname, String password) {
//		return (User) getCurrentSession().createQuery(
//				"from " + clazz.getName() + " where nickname='" + nickname + "' and password='" + password + "'").list()
//				.get(0);
//	}
//
//}
